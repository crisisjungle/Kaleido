# Simulation Playback

## Current Capability

Step 3 plays committed simulation checkpoints against one ledger-backed Timeline V3 global clock. The 2D graph and geographic map consume the same ordered events and playhead, but they use separate geometry renderers: the 2D graph preserves semantic/network structure, while the map only renders evidence-backed spatial placement and paths. Timeline V2 and legacy frames remain accepted through explicit compatibility projection.

Playback uses the network committed at the current round watermark as a stable, low-emphasis base plus a transient source → path → target wave. That committed base is never rebuilt or hidden between animation ticks; structures whose `first_seen_round` is still in the future remain genuinely hidden. Completed paths remain briefly as a quiet causal trail before returning to the committed base treatment, while only events whose global timing window is currently active receive the primary propagation focus.

The compact playback bar shows only the current progress rail, percentage, and one play/pause action. Connection deltas and new or active relationships belong to the observation views below, where they have enough room to be read. The Agent workbench and relationships/risk view consume the same append-only action, state, policy, relationship, and emergence artifacts as the round snapshot; they are not post-hoc UI narratives.

## Current Contract And V3 Migration

- `simulation-playback-timeline.v3` is the authoritative persisted playback contract. It owns one canonical, monotonic global clock, stable event-global timing, and ordered round segments. The frontend consumes that schedule; its internal `continuous-playback-plan.v1` is only a renderer projection normalized to a zero-based playhead, not a second persisted contract.
- A simulation `round` is a committed state checkpoint and a progress annotation. It is not an animation clip, restart boundary, renderer reset, or permission to clear the active/trail visual state. Crossing a round segment changes the displayed checkpoint while the global clock continues monotonically.
- Timeline V3 is additive to the existing animation payload. Legacy `frames` remain available as checkpoint snapshots and Timeline V2 remains accepted during migration. A V2 payload is compiled into one deterministic, in-memory global story schedule before playback starts; it must not be rescheduled independently each time a frame or round becomes current.
- V3 identity and head state live in `timeline_id`, `epoch_id`, and `head`. `clock` declares milliseconds, origin, canonical rate, committed duration, the 5,000 ms default populated-round duration, and the 900 ms empty-round duration. Each `rounds[]` segment carries `checkpoint_id`, event IDs/count, sequence/cursor bounds, and contiguous `start_ms` / `end_ms` / `duration_ms`.
- Every V3 event retains V2-compatible round-local `timing.start_ms` and adds the explicit alias `local_start_ms` plus authoritative `global_start_ms`, `global_end_ms`, and `duration_ms`. A child with an explicit persisted parent cannot start globally before that parent ends.
- V2 fallback timing is compatibility projection, not persisted truth. It preserves V2 event IDs, ledger order, typed path/evidence references, rounds, and explicit ancestry, but it must not fabricate a cross-round parent or claim that client-derived timing is canonical V3 timing. Its adaptive round segments target roughly 45–60 seconds for a typical ten-round dense replay.
- A V3 response is sufficient for replay and incremental live consumption without interpreting per-round `timing.start_ms` as a fresh zero point. Delta windows expose acknowledged/next/head cursors, head checkpoint/global time, returned segment bounds, and reset state. New live windows append committed schedule information to the existing global clock; already emitted later-round-safe event-global timing never shifts.
- Frozen V2 artifacts remain readable through the compatibility compiler. Moving a frozen fixture to embedded V3 requires an artifact-contract bump, deterministic regeneration, and validation; merely adding the V2 fallback compiler does not silently relabel an existing fixture as V3.

## Playback State Machine

Playback is evaluated from the global story cursor, not from a local timer that restarts for each frame.

1. **Scheduled:** the event is known but its global start has not been reached. It contributes no transient emphasis.
2. **Source:** the source node anticipates the change while the committed network remains visible beneath it.
3. **Path:** only the ordered `path_edge_ids` route grows from source to target. Related/mechanism evidence may remain subdued context but never becomes a later route segment.
4. **Arrival:** the target enters focus only after the wave reaches its response phase.
5. **Trail:** the completed event leaves a quiet `faded` causal trace for the configured trail window (currently 760 ms), and an explicit direct parent may be retained while its child is active. It is no longer counted as a current wave. After the trail expires, the entity remains in the committed base according to its frame lifecycle rather than staying as an event pulse forever.

Pause preserves the exact global cursor. Resume continues from it. Scrubbing reconstructs scheduled, active, arrived, trail, and committed-base state deterministically at the requested cursor. Reaching the final cursor stops playback without looping. Switching 2D/map views or changing observation tabs must not create a new cursor or replay the current round.

The renderer snapshot exposes `playback_global_elapsed_ms`, derived `playback_round_elapsed_ms` / `playback_round_duration_ms`, `playback_plan_duration_ms`, `playback_is_continuous`, `continuous_has_timeline`, `active_propagation_event_ids`, `trail_propagation_event_ids`, `recent_completed_propagation_events`, and `playback_started_event_count`. Graph, map, progress rail, and pulse panel must derive from that same snapshot.

## 2D And Map Playback

- Both renderers consume the same global cursor, active event IDs, trail event IDs, and checkpoint round. Their difference is geometry and foreground density, not event order.
- The 2D graph keeps its topology and layout stable. It can retain a broad low-emphasis network and uses the propagation overlay to show source/path/arrival order without restarting force layout or rebuilding the SVG at a round checkpoint.
- The map keeps the current camera and renders only physically supportable geography. Evidence-backed routes may grow and settle as geographic traces; schematic region links stay muted, while Agent and mixed synthetic relationships remain omitted from physical propagation.
- The shared foreground scheduler exposes at most three simultaneous active waves and three subdued trail events to the 2D graph. The map consumes the same schedule/order but narrows its large-distance foreground to at most two grounded active routes and two trail routes. These renderer caps preserve phase diversity and do not change authoritative timing. The right-side pulse panel separately receives the cumulative started-event count and up to six recent completed relationships.
- A completed route settles to a quiet trace. A short event gap or a round-segment boundary must not blank the committed base, relight a completed wave as active, or snap the map camera.

## Live And Replay

- **Live:** only completed rounds are appendable. Incremental reads merge by stable event/frame identity and advance the canonical cursor/round segments without exposing an unfinished snapshot. Playback may wait at the latest committed cursor while the backend runs; waiting is not an ended replay and must not synthesize a fallback event.
- **Replay:** the full persisted V3 schedule is authoritative. A V2 or legacy frozen artifact is compiled once through the compatibility boundary, then uses the same global-cursor state machine as a formal V3 replay.
- The live client sends its current `timeline_id` with cursor/round watermarks. A `reset_required` delta is never merged into the old epoch; the client fetches one fresh full snapshot. A live buffer may start automatically when it grows from one frame to two, but later polling must not pull a user-paused or scrubbed playhead forward.
- Frozen replay may auto-start once after loading. An explicit pause or scrub cancels delayed auto-start, and replay reaches the end without looping; pressing play again from the end is the only action that returns the playhead to zero.
- Live and replay share pause, resume, scrub, 2D/map switching, committed-base/trail treatment, and end-of-timeline behavior. Their only intended difference is that live playback can receive later committed schedule windows.
- Route changes and component unmounts do not stop the backend run. Returning to Step 3 restores the latest available committed timeline; only an explicit stop operation terminates execution.

## Acceptance And Known Risks

Required acceptance checks:

- The progress rail advances continuously across round checkpoints; the visible round label may change, but elapsed playback time never resets to zero.
- No round boundary produces an empty graph/map frame, clears prior committed entities, resets the playhead, or restarts the current route.
- A source/path/target chain is readable in order, and the target does not focus before arrival. Completed paths settle while a later wave begins on top of the same stable state.
- Pause/resume preserves the cursor. Scrubbing backward removes future entities and future event trails; scrubbing forward deterministically restores them.
- Switching map/2D during playback preserves global progress and does not restart, skip, or duplicate events. Map zoom/center and 2D pan/zoom remain user-controlled.
- Live incremental reads do not shift an already emitted global start, expose an unfinished round, or duplicate a committed event. Replay stops once at the final cursor.
- Wuhan replay passes the same continuity checks as a normal generated simulation and remains byte-deterministic after any required artifact regeneration.

Known risks and migration limits:

- Timeline V2 ledgers do not consistently persist cross-stage ancestry. The fallback scheduler can preserve explicit parents and deterministic phase order, but it cannot turn independent roots into one causal chain.
- A V2 compatibility schedule remains client-derived. Cross-client and long-term replay timing becomes authoritative only after the artifact embeds Timeline V3 global timing and round segments.
- The pulse panel shows a cumulative started-relationship count, the current active/trail window, and at most six recent completed relationships. This is a readable playback summary, not the complete audit ledger; the full event collection remains in the timeline contract.
- V3 projection is append-stable for newly committed later rounds. Preventing a late write into an already committed round still depends on a persisted runtime commit index; the read-side projector does not invent that boundary.
- Physical map continuity is limited by geographic grounding. Missing or synthetic routes may remain schematic or absent even when the 2D topology can show the same business relation.
- Very dense independent event sets still require foreground sampling. Sampling must preserve audit completeness and phase diversity, and it must not be mistaken for causal omission in the underlying ledger.

## Key Rules

- Runtime intervention text follows `docs/modules/semantic-input.md`. `/api/simulation/inject` resolves natural-language targets against the current catalog before writing runtime state, preserves an explicit `start_round=0`, and uses a persisted per-draft idempotency receipt so retries cannot duplicate a runtime mutation.
- Accepted normalized interventions enter the activity/history list directly. Model, repair, fallback, and unresolved-target details stay in semantic audit and system logs.
- The global workflow shell owns the Step 3 title and run status. Step 3 must not repeat them as a local hero or as cards for round, stage, scenario state, or simulated time.
- Runtime playback is one compact row: a single progress rail, one percentage, and one play/pause button. The rail already supports direct scrubbing, so previous/next/latest buttons are not separate controls.
- Playback delay and the state-heat sorting key are implementation defaults, not user decisions. Do not expose millisecond speed or a generic “state dimension” selector in the playback bar.
- The compact playback row and observation tabs remain visible above the independently scrolling workspace. Their combined height should stay below 120 px in the standard split workbench.
- Cross-step actions remain in the shared compact bottom action bar. Runtime intervention stays a scoped action there and opens its drawer without enlarging the playback row.
- Visible round labels, distance bands, uncertainty labels, status messages, and intervention results must use the shared Chinese display boundary. Unknown enums, numeric-only identifiers, exception text, paths, and network addresses are not valid display fallbacks.
- Start, stop, inject, status, configuration, action, and timeline JSON responses share the same backend response boundary. Runtime control failures may keep machine error codes, but their visible message is Chinese and never includes traceback or raw exception detail.
- Step 3 has five peer observation views: current-round evolution, spatial state, propagation mechanism, subject response, and risk objects. Dynamic relationships and Agent lifecycle belong to subject response; risk objects remain independent. Do not merge relations and risk back into one view.
- Every runtime round follows one fixed order: activate due Agents; apply variables and diffusion; validate and execute Agent actions; update relationship coupling and mechanism propagation; execute policies; calculate feedback; detect Agent emergence; persist the round snapshot; then detect and refresh risks. Risk results must never be fed back into the earlier Agent decision stages.
- Agent actions must pass capability, permission, resource, lifecycle, and prerequisite checks. Proposed but rejected actions remain visible as audit outcomes and produce no unauthorized state delta.
- Relationship state changes only through recorded interaction evidence. The runtime stores trust, dependency, coordination, and tension plus the `RelationshipEvent` that explains each change; co-location or a shared risk reference alone is insufficient.
- Runtime Agent emergence resolves capability gaps in the order dormant activation, aggregate split, then provisional creation. New or split Agents become actionable from the next round, and all activation, creation, lineage, and candidate outcomes remain append-only.
- Policy execution consumes the prepared `PolicyExecutionPlan`. A policy without a qualified executor or satisfied trigger remains pending/unbound; it must not create an execution result merely because the user entered a policy intent.
- Runtime intervention is a write operation and lives in a separate drawer with new, active, and historical intervention states. It is not an observation panel.
- System logs are internal diagnostics and must not appear in the normal Step 3 business interface.
- The animation response keeps legacy `frames` for checkpoint compatibility and exposes `timeline.contract_version=simulation-playback-timeline.v3` with `compatible_contract_versions=[simulation-playback-timeline.v2]`. Timeline events come from the spread, Agent interaction, dynamic-edge, and relationship-event ledgers before any frame fallback is considered.
- Every V3 timeline event carries a stable ID, global and per-round sequence, phase/kind, source/target references, typed edge references, evidence-backed parent references, round-local and global timing, intensity/confidence, grounding, and Simplified-Chinese display fields. `path_edge_ids` is the strictly ordered, directionally continuous route that may be staggered from source to target; `related_edge_ids` contains affected relationships or mechanism/evidence references and never becomes a later route segment. `edge_ids` remains only the ordered compatibility union of those disjoint fields. Explicit, resolvable ledger ancestry may additionally expose `root_event_id` and `hop`; unresolved roots are not projected. Missing ledgers remain explicit in timeline grounding.
- Live cursor reads use a non-negative `after_cursor` or `since_cursor`. Only completed rounds enter the live timeline, layout history, and frame inputs, so an emitted sequence never shifts and an unfinished round cannot expose phantom relationships. Cursor responses omit historical frames; an empty window also omits layout. Frozen/legacy artifacts may expose `legacy_frame`, but it is marked `legacy_fallback`; a live run waiting for its first committed ledger keeps cursor `0`, and Agent numeric IDs are never used as propagation order.
- A ledger-provided single main relationship (`edge_id`, `relationship_edge_id`, or `relationship_contract_id`) is authoritative and may form one path segment only when it resolves between the event source and target; it must not expand into every parallel source-target edge. Explicit multi-edge paths must pass directional continuity. Diffusion events without an explicit edge prefer one matching transport edge over a generic neighbor relation. Mechanism edges and other supporting references remain `related_edge_ids`: they may stay visible as subdued context but are never staggered as propagation. Untyped ledger or legacy `edge_ids` is path-compatible only when it contains one edge or declares an ordered-path contract; an untyped multi-edge set remains related evidence. Persisted V2 paths are revalidated against the current layout before playback, so a stale or directionally broken path is demoted to related evidence instead of being staggered. A legacy interaction that proves only its endpoint pair chooses one stable candidate path and marks ambiguous matches `partial`, rather than animating every parallel edge. Historical dynamic edges referenced by committed timeline events stay in the animation layout after dormancy or expiry, without being reactivated in current runtime state.
- Parent links are projected only from persisted causal references. The current ledgers do not consistently write a cross-stage `root_event_id` or `hop`, so Timeline V3 currently guarantees deterministic phase waves and globally ordered explicit parents, not a complete causal tree; the projection layer must not invent missing ancestry from timestamps, shared endpoints, or visual proximity.
- The Wuhan frozen fixture is an explicit exception only for its curated environment-diffusion source data: it deterministically projects the existing Jianghan outbreak variable over configured directed transport edges and cumulative travel times. These events are labeled `golden_fixture_projection` / `curated_deterministic_fixture`, never `observed`, and do not make Agent responses causal children.
- Playback frame changes must not rebuild the 2D graph when the node and edge structure is unchanged.
- User zoom, pan, and drag state on the graph must remain usable while the animation is playing.
- The base graph should stay spatially stable; frame changes should update visual state and focus highlighting only.
- 2D semantic zoom is clamped to `0.5–5`. Below `0.7`, ordinary Agent labels enter level-of-detail hiding; active sources/targets, selected nodes, risk nodes, regions, and priority nodes remain readable. Node glyphs and text do not scale as one SVG image.
- The 2D density modes cap and score baseline relationships by evidence, confidence, strength, broad node coverage, and per-node fan-out. Parallel same-semantic relationships are bundled. The active propagation overlay is independent of that baseline cap, so a real current event cannot disappear because its edge was outside the initial top-N.
- 2D Agents stay constrained around their owning region. Intra-region Agent links use compact distances; cross-region propagation remains visible as a deliberate cluster-to-cluster jump instead of turning local interactions into full-screen lines.
- Timeline playback keeps the full event ledger for audit. The continuous 2D foreground renders at most three simultaneous current-wave events plus three trail events; the map narrows those shared candidates to two grounded current routes plus two trail routes. The older frame-local compatibility helper retains its 18-event snapshot sampling limit. Dense sampling and foreground caps preserve phase diversity; active events are selected round-robin by phase so a still-running environment front cannot disappear merely because later Agent or relationship events start.
- The Step 3 pulse ledger and the graph/map wave consume the same `propagation_events` and active IDs. The business panel must not report a non-zero current wave while the visual surface is still using an unrelated legacy focus set.
- A target node must not enter focus when its event merely starts. The source may pulse first, the ordered path then grows, and the target becomes focused only in the arrival/response phase; this keeps the visual order causal instead of lighting both endpoints before the route is traversed.
- A short gap between waves keeps only the low-emphasis base network; it must not relight the last completed event as though it were still active.
- The map uses fractional cursor-anchored wheel zoom with a request-animation-frame easing loop. It does not use Leaflet's integer/debounced pulse zoom. Animation-layer reconciliation must preserve the user's current zoom and center; content fitting is allowed only for initial content or an explicit projection identity change.
- The Leaflet external-viewport watcher compares a primitive latitude/longitude/zoom signature, not projection-object identity. Per-frame animation projection replacement therefore cannot reset a user-controlled fractional zoom, including across round changes.
- The animation `layout` preserves the source map projection's `source_mode`, `map_seed_id`, `geographic_grounding`, `data_quality`, `selection_summary`, and `meta`. Step 3 must carry those fields through its animation-map adapter, including an explicit null seed ID, rather than recreating an ungrounded map DTO from coordinates alone.
- Map overlays reconcile by stable feature ID instead of clearing and recreating every layer on each animation frame.
- A map relationship uses supplied LineString/MultiLineString/route geometry when available. With only two real geographic endpoints it uses one honest straight segment; the renderer must not invent random bend points or polygon-like detours.
- Synthetic Agent and mixed synthetic/geographic relationships are omitted from physical propagation on the map. A synthetic region-to-region relationship may appear only as a muted dashed schematic and cannot receive the primary physical wave treatment.
- `curated_deterministic_fixture` is a narrow replay-only spatial grounding, not an alias for real or map-seed geography. Its primary map wave is allowed only when both nodes explicitly carry finite fixture coordinates and the referenced edge is an explicitly fixture-grounded `transport_edge`; other fixture relationships remain schematic or omitted.
- Steady items already committed at the current watermark remain visible at low emphasis during Timeline V3 or V2-compatible playback. Backend `hidden` and `first_seen_round > current_round` remain invisible in both 2D and map renderers, even if a stale focus/highlight reference names them; `hidden` must not be promoted to `steady` or `faded` merely to preserve context between waves.
- Graph inspection state obeys the same watermark. When scrubbing backwards, a selected node, edge, parallel-edge group, or self-loop group is reconciled against the current projected graph; if every selected entity is absent or `hidden`, its label emphasis and detail panel are cleared before rendering.
- Step 3 should prefer the animation payload layout while replaying frames so node and edge IDs match the frame state contract.
- Replay playback stops at the last frame. It should not loop back to the first frame automatically, because that reads as a sudden refresh.
- Route changes and component unmounts do not stop a backend run. Only the explicit stop operation may terminate it, so users can leave Step 3 and return without losing an in-progress simulation.
- Wuhan frozen replay and normal generated simulations must use the same Step 3 playback behavior.
- Step 3 reuses the immutable V2 definitions from Step 2. Selecting a risk changes only the local viewing focus and highlights its real mechanism nodes/edges; selection must not change simulation state or Agent behavior.
- Risk focus may highlight every referenced mechanism node and edge in the relationship graph, but the map renders only references with real geographic anchors. Playback must not synthesize coordinates for process, evidence, family, color, or other non-geographic nodes.
- The risk view shows the causal statement, dedicated tension trace, runtime status, created round, evidence state, dedicated metrics, emergence/status events, and primary-risk switches. Main families are filters/report groups, not a second visible risk ontology.
- The subject-response view shows lifecycle, representation level, capabilities, relative resource state, latest validated action, current relation state, relation events, and Agent activation/split/provisional-creation events. The separate risk-object view reads those outcomes and cannot create Agents.
- Any returned risk collection uses one compact horizontal selector. The panel renders details only for the selected object instead of stacking a vertical card wall above the detail area, and the frontend does not assume the current active-risk cap is a permanent display limit.
- Risk navigation buttons are conditional on actual horizontal overflow. When all objects fit there are no arrow controls; when they do not fit, two small previous/next buttons supplement trackpad, wheel, and touch scrolling. Narrow viewports keep the same rail and never switch to a native dropdown. Empty monitoring, lifecycle, and quality sections stay hidden so they do not reserve blank vertical space.
- Newly emerged risks appear automatically after the runtime detector promotes them. Dormant and resolved definitions remain available as history, while no more than eight risks are active.
- When no active risk remains, `primary_active_risk_id` is empty. A resolved object must not remain primary; if a later snapshot validly returns an object to watch/elevated/critical, primary selection may resume under the normal five-point hysteresis rule.
- A forced rerun first closes the retained command environment, then clears prior round snapshots, actions, risk runtime/events, Agent interactions and decisions, dynamic/relationship events, reasoning, emergence, lineage, candidate runtime ledgers, and injection receipts before round zero. It preserves the prepared immutable V2 definitions and their preparation-time candidate audit. One simulation may have only one live IPC consumer.
- Server shutdown may terminate a lingering child environment after both platforms have emitted their completion records, but it must preserve the successful `completed` run state, completion timestamp, round count, and platform completion flags. Shutdown cleanup marks only genuinely unfinished runs as `stopped`.
- The affected-subject display uses real entity/Agent references and shows the receptor-role match label as provenance. Machine scope keys and primary-region IDs remain available to the client but are translated or omitted from visible text.
- A zero-risk simulation remains a valid playback/report state and shows the evidence-gated empty message instead of a placeholder object.
- On viewports up to 760px, the shared workflow shell shows the Step 3 workbench and graph as separate full-width local views. The graph toggle switches between them without changing the simulation route, selected risk, or runtime state.
- Legacy frozen artifacts may be projected through the V2 compatibility boundary, but the Wuhan fixture is versioned and regenerated whenever the formal animation contract changes so `/demo/wuhan` remains a faithful fast fixture for the shared flow.
- The current Wuhan animation artifact embeds Timeline V3 directly. Its two frozen copies must remain byte-deterministic and must pass global-clock/round-segment, event-order, parent timing, reference-integrity, frame-linkage, and rebuild-hash validation.
- Wuhan validation additionally requires a non-empty `environment_diffusion` phase, one resolvable root-to-multi-hop chain, complete source/target/transport-edge references, parent/root/hop continuity, and in-range arrival rounds derived from each edge's `travel_time_rounds`.

## Maintenance Entry

- Step 3 playback workbench: `frontend/src/components/KaleidoStep3.vue`
- Shared graph renderer: `frontend/src/components/GraphPanel.vue`
- Map relationship renderer: `frontend/src/components/MapRelationPanel.vue`
- Persistent Leaflet surface and smooth zoom: `frontend/src/components/LeafletMapPicker.vue`
- Shared playback projection and wave scheduler: `frontend/src/utils/simulationPlayback.js`
- Map geometry normalization and slicing: `frontend/src/utils/mapRelationGeometry.js`
- Step 3 route shell and animation data adapter: `frontend/src/views/SimulationRunView.vue`
- Animation payload builder: `backend/app/services/simulation_animation_service.py`
- Timeline projection contract tests: `backend/tests/test_simulation_animation_timeline.py`
- Risk runtime tracker and events: `backend/app/services/risk_runtime_tracker.py`, `backend/app/services/risk_event_engine.py`, `backend/app/services/risk_emergence_detector.py`
- Agent action and state contracts: `backend/app/services/agent_action_contract.py`, `backend/app/services/agent_state_mutation.py`
- Relationship runtime: `backend/app/services/agent_relationship_runtime.py`
- Policy runtime: `backend/app/services/policy_runtime.py`
- Agent emergence: `backend/app/services/agent_emergence_detector.py`
- Runtime orchestration and artifact writing: `backend/scripts/run_envfish_simulation.py`, `backend/app/services/simulation_runner.py`

## History

- 2026-05-23: Reworked Step 3 playback so round changes update graph animation styles in place instead of resetting the 2D graph. Added per-round connection deltas and stopped replay from jumping back to the first frame at the end.
- 2026-05-23: Tightened playback into a focused propagation pulse. Step 3 now uses animation layout IDs during replay, progressively reveals current-frame nodes and edges, and hides non-current background relationships in graph and map modes.
- 2026-07-12: Reorganized Step 3 into persistent runtime controls plus five peer observation views. Moved runtime intervention into a dedicated drawer, removed system logs from the business UI, and made the runtime graph visible by default.
- 2026-07-12: Added V2 risk lifecycle playback with dedicated metrics, evidence state, created rounds, automatic emergence, resolved/dormant history, primary-risk switch events, and real graph-path focus only.
- 2026-07-13: Restricted risk-path map focus to genuinely geographic references, added full-width mobile workbench/graph switching, and documented that Wuhan fixture migration is intentionally deferred while the formal V2 path ships.
- 2026-07-13: Unified runtime progress, compact observation tabs, green control states, responsive grids, and the shared bottom action bar with the other workflow steps. Runtime narratives, grounding notes, and errors now pass through the same Chinese display boundary as graph and report content.
- 2026-07-13: Hardened runtime fallbacks for unknown stages, distance bands, uncertainty labels, numeric-only region references, and technical errors so raw backend values cannot become labels or broken punctuation fragments.
- 2026-07-13: Added blueprint-wide response projection for runtime control and history endpoints, plus safe public report-log summaries that omit tool calls, parameters, prompts, model output, paths, addresses, and internal IDs.
- 2026-07-13: Collapsed the oversized Step 3 runtime console into one progress-and-play row. Removed duplicate status cards, previous/next/latest, the state-dimension and millisecond-speed selectors, the duplicated matrix score column, and the disabled completed-state stop action so the observation workspace receives the vertical space.
- 2026-07-13: Made navigation non-destructive, hardened forced-rerun cleanup across runtime ledgers, preserved successful run state when shutdown cleans a lingering environment, removed resolved objects from primary-risk selection, and exposed receptor-role provenance for differentiated affected subjects in the shared Step 2/3 risk view.
- 2026-07-13: Replaced the Step 3 vertical risk-card wall and oversized risk summary cards with a compact horizontal selector, selected-object-only detail, and progressive disclosure for empty monitoring, lifecycle, and quality sections. The selector now keeps one interaction at every width and shows mouse navigation buttons only when it actually overflows.
- 2026-07-13: Added the Agent V2 round contract: capability/permission/resource action validation, append-only state mutations, evidence-backed relationship state/events, concrete policy execution, dormant activation, same-archetype aggregate split with resource conservation, cross-archetype provisional Agent creation, and the fixed risk-after-Agent ordering. Added corresponding Agent workbench and relationship/lifecycle explanations to the shared Step 3 UI.
- 2026-07-13: Added the additive playback timeline V2 contract over existing frames. Real append-only ledgers now determine propagation order and grounding, completed-round cursors support incremental reads, legacy runs receive an explicit fallback instead of fabricated Agent-ID reveal batches, and each frame carries its matching timeline event IDs.
- 2026-07-14: Rebuilt graph playback around a stable base plus a bounded source/path/target wave. Added ledger-backed incremental playback, shared graph/pulse event state, in-place 2D updates, a dedicated propagation overlay outside density filtering, region-cluster constraints, `0.5` semantic zoom floor, and low-zoom label LOD.
- 2026-07-14: Replaced random map detours and per-frame overlay resets with evidence-backed geometry, stable layer reconciliation, sliced current-wave paths, projection-aware synthetic placement rules, camera-preserving updates, and continuous fractional cursor-anchored wheel zoom.
- 2026-07-14: Bumped and deterministically regenerated the Wuhan golden artifact so both frozen animation files embed Timeline V2 directly, with deterministic ledger order, complete frame event references, and no Agent-ID-based reveal batching.
- 2026-07-14: Added the Wuhan fixture's curated Jianghan-rooted diffusion tree over explicit directed transport edges. The embedded timeline now exposes 12 environment-diffusion events through five hops without claiming observation or fabricating an Agent-response ancestry.
- 2026-07-14: Preserved spatial provenance through animation-layout map reconstruction and added the narrow curated-fixture route policy, so Wuhan transport waves animate on deterministic coordinates while synthetic and non-route relationships remain excluded from physical propagation.
- 2026-07-13: Hardened Timeline V2 cursor and edge truth: live waiting no longer consumes the first cursor with a fallback frame, explicit and transport edges stay unique, dormant historical edges remain renderable, unfinished-round artifacts remain hidden until snapshot commit, and incremental windows no longer resend all historical frames.
- 2026-07-14: Made runtime injection idempotent per edited draft and preserved explicit round zero through the frontend/API boundary; replay details remain system-log-only.
- 2026-07-14: Split Timeline V2 edge references into ordered continuous `path_edge_ids` and non-propagating `related_edge_ids`. Dynamic/relationship events still grow their one resolved main relationship, while mechanism/evidence collections can no longer masquerade as sequential propagation hops; legacy multi-edge focus sets remain association context unless explicitly ordered.
- 2026-07-14: Closed final playback-order leaks: future structures now obey the round watermark in both renderers, targets wait for the arrival phase before focusing, and Leaflet watches primitive viewport values so animation RAF updates and round changes cannot snap fractional zoom back to `zoom_hint`.
- 2026-07-14: Bound graph selection and detail inspection to the current playback watermark, so rewinding before an entity's first-seen round clears hidden node, edge, parallel-group, and self-loop selections instead of leaking future labels or attributes.
- 2026-07-14: Replaced the per-round playback timer with one RAF-driven global playhead. Added authoritative Timeline V3 global timing and checkpoint segments, deterministic V2 continuous fallback pacing, pause/scrub continuity, active-to-trail handoff, cumulative pulse counts with recent completion history, timeline-identity reset recovery, and separate 2D/map foreground caps without changing shared event order.
- 2026-07-15: Wuhan V2 keeps the same 203000 ms global cursor across 37 frames and adds six chapter seek points. R0 is reconstructed from the frozen configuration baseline; it must never fall back to the latest R36 snapshot, and every cumulative runtime collection must use `0` as a real upper watermark rather than treating it as an absent value. R1–R36 each expose four curated primary event cards while all 696 events remain in the timeline ledger.
